name: family_finance_app
description: "Family Finance App - Gen Z Premium Edition"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management
  provider: ^6.1.1

  # UI & Animations
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  smooth_page_indicator: ^1.2.0
  table_calendar: ^3.0.0

  # Charts & Graphs
  fl_chart: ^0.66.2

  # Utilities
  intl: ^0.19.0
  vibration: ^3.1.5

  # Icons
  cupertino_icons: ^1.0.6

  # Storage
  shared_preferences: ^2.2.2
  sqflite: ^2.3.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
